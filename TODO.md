# TODO

- Keep structured composer history available while session scrollback is being fetched or replayed. The Android client currently retains the old rendered transcript during reconnects, but clears composer history and rebuilds it from replay events; history navigation is therefore empty or incomplete until replay catches up.
