<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <!-- Agent UI brand mark: the accent "status dot" with its soft glow ring,
         the same motif used before the wordmark in the web front-end. -->

    <!-- Outer glow ring (accent at low opacity). -->
    <path
        android:fillColor="#24D97757"
        android:pathData="M54,30 a24,24 0 1,0 0.001,0 z" />

    <!-- Mid ring. -->
    <path
        android:fillColor="#40D97757"
        android:pathData="M54,38 a16,16 0 1,0 0.001,0 z" />

    <!-- Solid accent core. -->
    <path
        android:fillColor="#D97757"
        android:pathData="M54,44 a10,10 0 1,0 0.001,0 z" />
</vector>
